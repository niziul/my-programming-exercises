/*
    Exemplos de cometários descritivos, embora não necessários:
*/

// Importações
using System;

// Namespace
namespace MeuAppComments
{
    // Classe principal do programa

    // Anotações XML:
    
    /// <properties>
    ///
    class Program
    {
        // Função principal do programa
        static void Main(string[] args)
        {
            /*
                Usando as teclas de atalho: "Ctrl" + "K" + "C" é possível realizar um cometário de uma única linha.
                Para desfazer o cometário utilize as teclas "Ctrl" + "K" + "U"
            */

            // var text = "Testing"; 



            var text = "Testing";
            Console.WriteLine(text.ToLower());          
        }       
    }
}