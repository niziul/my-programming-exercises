#include <iostream>

using std::system;

int main(int argc, char** argv)
{
	system(argv[1]); // run the calculator

	//system(argv[2]); // run the notepad

	//system(argv[3]); // run the mspaint

	return 0;
}